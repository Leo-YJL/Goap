using UnityEngine;

namespace ReGoap.Unity.FSMExample.OtherScripts {

    /// <summary>
    /// 原始资源
    /// </summary>
    public class PrimitiveResource : MonoBehaviour {
        // Start is called before the first frame update
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}